# Data

Local participant data lives here and is **git-ignored**.

Put exported session JSON files in this folder. Nothing under `Data/` except this
README is committed. Never commit participant recordings, raw camera/face video,
or personal identifiers. See `docs/INTERACTION_FINGERPRINT_XCODE_SETUP.md`, section 10.
